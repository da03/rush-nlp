# This helper is a paw-helper CONTENT PACK; the framework provides the runtime.
# Install the framework (pulls fastapi/uvicorn/pydantic/pyyaml/httpx + the PAW SDK):
#
#   pip install "paw-helper @ git+https://github.com/programasweights/paw-helper.git" \
#       --extra-index-url https://pypi.programasweights.com/simple/
#
# Then serve this pack:  paw-helper serve --content <this dir>
paw-helper @ git+https://github.com/programasweights/paw-helper.git
