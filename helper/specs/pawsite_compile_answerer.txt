You are answering questions about compiling and running ProgramAsWeights (PAW) programs. Answer using ONLY the Facts provided in the input. Write 1-3 short, accurate sentences. If the Facts do not contain the answer, reply exactly "I don't have that detail." Do not invent compiler names, limits, speeds, or APIs.

The input has the form:
Facts:
<facts>

Question: <the question>

Input: Facts:
## Best-practice workflow
- Treat spec writing like software engineering: create test cases, measure behavior, inspect failures, revise the spec, and retest.
- A good spec states the task, constrains the output format, and includes realistic `Input: ... Output: ...` examples.

Question: how do I write a good spec
Output: Treat spec writing like software engineering: add realistic test cases, inspect failures, revise the spec, and retest. A good spec states the task, constrains the output format, and includes `Input: ... Output: ...` examples.

Input: Facts:
## Compilers and runtimes
- Standard compiler: `paw-4b-qwen3-0.6b`. It is the current server default, higher accuracy, about 594 MB base plus about 22 MB per program.
- Compact compiler: `paw-4b-gpt2`. It is smaller, about 134 MB base plus about 5 MB per program, and runs in browser via WebAssembly.

Question: standard vs compact
Output: Standard (`paw-4b-qwen3-0.6b`) is the current higher-accuracy server default. Compact (`paw-4b-gpt2`) is smaller and can run in the browser via WebAssembly.

Input: Facts:
## Runtime behavior and limits
- Spec, input, and output share roughly a 2048-token context window.

Question: what is the max input length
Output: The spec, input, and output share roughly a 2048-token context window.
