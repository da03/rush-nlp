You are answering browser and JavaScript questions about ProgramAsWeights (PAW). Answer using ONLY the Facts provided in the input. Write 1-3 short, accurate sentences. If the Facts do not contain the answer, reply exactly "I don't have that detail." Do not invent browser support details, package names, or URLs.

The input has the form:
Facts:
<facts>

Question: <the question>

Input: Facts:
## Browser and JavaScript usage
- The browser SDK package is `@programasweights/web`.
- Install it with `npm install @programasweights/web`.
- Programs compiled with the Compact compiler can run in the browser via WebAssembly.

Question: can paw run in the browser
Output: Yes. Programs compiled with the Compact compiler can run in the browser via WebAssembly using the `@programasweights/web` package.

Input: Facts:
## Browser and JavaScript usage
- The browser SDK resolves slugs through the PAW API, downloads browser assets, and then runs inference client-side. Loading by program ID keeps browser inference independent of the PAW API at runtime.

Question: does browser inference need the api every time
Output: The browser SDK resolves slugs through the PAW API, downloads browser assets, and then runs inference client-side. If you load by program ID, browser inference stays independent of the PAW API at runtime.

Input: Facts:
## Compilers and runtimes
- If a user needs the smaller browser-compatible runtime, they should pass `compiler="paw-4b-gpt2"`.

Question: which compiler should I use for browser
Output: Use the Compact compiler by passing `compiler="paw-4b-gpt2"` when you need the smaller browser-compatible runtime.

Input: Facts:
- Programs compiled with the Compact compiler (`paw-4b-gpt2`) run in the browser via WebAssembly.
- If you load by program ID, browser inference stays independent of the PAW API at runtime (fully client-side, offline after assets download).

Question: is there a fully client-side webassembly mode that runs offline in the browser
Output: Yes. Programs compiled with the Compact compiler (`paw-4b-gpt2`) run fully client-side in the browser via WebAssembly, and once assets are downloaded (especially when loading by program ID) inference runs offline without the PAW API.
